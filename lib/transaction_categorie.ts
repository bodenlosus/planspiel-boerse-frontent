export default class TransactionCategorie{
    color: string
    name: string
    display: string
    constructor(color: string, name:string, display:string){
        this.color = color
        this.name = name
        this.display = display
    }
}